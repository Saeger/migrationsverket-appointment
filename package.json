{
  "name": "migrationsverket-appointment",
  "version": "2.0.0",
  "description": "An alarm that get's dispatched whenever a new appointment slot is available",
  "main": "src/index.ts",
  "scripts": {
    "compile": "rm -rf dist && tsc",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Márcio Saeger",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.4",
    "beepbeep": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^18.18.9",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
